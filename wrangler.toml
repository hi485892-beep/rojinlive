name = "rojin-live-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# 배포 후 다음 명령으로 API 키를 시크릿으로 등록하세요 (이 파일에 직접 쓰지 마세요):
# npx wrangler secret put ANTHROPIC_API_KEY

# 선택: 본인 GitHub Pages 주소로 CORS를 제한하고 싶다면 아래 주석 해제 후 값 수정
# [vars]
# ALLOWED_ORIGIN = "https://your-username.github.io"
